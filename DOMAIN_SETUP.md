# Настройка домена для Face Crop сервиса

## Ваши данные сервера

- **IP адрес**: `213.32.16.119`
- **IPv6 адрес**: `2001:41d0:305:2100::f976` (опционально)

## Настройка DNS записей

### В Cloudflare (или другом DNS провайдере)

Зайдите в панель управления DNS вашего домена и добавьте следующие записи:

#### 1. Основной домен (example.com)

| Тип | Имя | IPv4 адрес | TTL | Прокси |
|-----|-----|------------|-----|--------|
| A | `@` | `213.32.16.119` | Auto | 🟡 Off (серый) |

**Важно**: Отключите прокси (серый облачко) в Cloudflare, чтобы IP адрес был виден напрямую.

#### 2. Поддомен www (www.example.com)

| Тип | Имя | IPv4 адрес | TTL | Прокси |
|-----|-----|------------|-----|--------|
| A | `www` | `213.32.16.119` | Auto | 🟡 Off (серый) |

#### 3. Поддомен для API (опционально)

Если хотите использовать поддомен типа `crop.example.com` или `api.example.com`:

| Тип | Имя | IPv4 адрес | TTL | Прокси |
|-----|-----|------------|-----|--------|
| A | `crop` | `213.32.16.119` | Auto | 🟡 Off (серый) |

#### 4. IPv6 (опционально)

Если хотите поддержку IPv6:

| Тип | Имя | IPv6 адрес | TTL | Прокси |
|-----|-----|------------|-----|--------|
| AAAA | `@` | `2001:41d0:305:2100::f976` | Auto | 🟡 Off (серый) |

## Проверка DNS записей

После добавления записей подождите 5-15 минут для распространения DNS, затем проверьте:

```bash
# Проверка A записи
dig example.com +short
# Должен вернуть: 213.32.16.119

# Или через nslookup
nslookup example.com
```

## Настройка Nginx на сервере

После того как DNS записи настроены, настройте Nginx на сервере:

### 1. Установка Nginx

```bash
ssh root@213.32.16.119
apt install nginx -y
```

### 2. Создание конфигурации

```bash
nano /etc/nginx/sites-available/face-crop
```

Вставьте следующее (замените `yourdomain.com` на ваш домен):

```nginx
server {
    listen 80;
    server_name yourdomain.com www.yourdomain.com;

    location / {
        proxy_pass http://localhost:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # Увеличение лимита для загрузки файлов
        client_max_body_size 50M;
    }
}
```

### 3. Активация конфигурации

```bash
# Создать символическую ссылку
ln -s /etc/nginx/sites-available/face-crop /etc/nginx/sites-enabled/

# Удалить дефолтную конфигурацию
rm -f /etc/nginx/sites-enabled/default

# Проверить конфигурацию
nginx -t

# Перезапустить Nginx
systemctl restart nginx
systemctl enable nginx
```

### 4. Настройка HTTPS (SSL)

```bash
# Установка Certbot
apt install certbot python3-certbot-nginx -y

# Получение SSL сертификата
certbot --nginx -d yourdomain.com -d www.yourdomain.com

# Следуйте инструкциям на экране
```

Certbot автоматически:
- Получит SSL сертификат от Let's Encrypt
- Обновит конфигурацию Nginx для HTTPS
- Настроит автоматическое обновление сертификата

## Проверка работы

### 1. Проверка через IP
```bash
curl http://213.32.16.119:8000/health
```

### 2. Проверка через домен (после настройки DNS)
```bash
curl http://yourdomain.com/health
```

### 3. Проверка через браузер

Откройте в браузере:
- `http://yourdomain.com` (или `https://yourdomain.com` после настройки SSL)

Вы должны увидеть интерфейс загрузки фотографий.

## Troubleshooting

### DNS не работает

1. **Проверьте DNS записи**:
   ```bash
   dig yourdomain.com
   nslookup yourdomain.com
   ```

2. **Убедитесь, что прокси отключен** в Cloudflare (серый облачко, не оранжевый)

3. **Подождите распространения DNS** (может занять до 24 часов, обычно 5-15 минут)

### Nginx не работает

1. **Проверьте конфигурацию**:
   ```bash
   nginx -t
   ```

2. **Проверьте статус**:
   ```bash
   systemctl status nginx
   ```

3. **Проверьте логи**:
   ```bash
   tail -f /var/log/nginx/error.log
   ```

### Приложение не доступно

1. **Проверьте, что контейнер запущен**:
   ```bash
   docker compose ps
   ```

2. **Проверьте логи**:
   ```bash
   docker compose logs
   ```

3. **Проверьте, что порт 8000 открыт**:
   ```bash
   netstat -tulpn | grep 8000
   ```

## Быстрая проверка всех настроек

Выполните на сервере:

```bash
# Проверка Docker
docker --version
docker compose ps

# Проверка Nginx
nginx -t
systemctl status nginx

# Проверка портов
netstat -tulpn | grep -E '8000|80|443'

# Проверка приложения
curl http://localhost:8000/health
```

Все должно работать! 🎉
