# 🚀 Настройка автоматического деплоя (один раз)

## Шаг 1: Выполните команду через KVM (ОДИН РАЗ)

Скопируйте и вставьте эту команду в KVM консоль:

```bash
sudo mkdir -p /root/.ssh && sudo chmod 700 /root/.ssh && echo "ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIMt02FyJUnNTf0l48oBSLb96QKc8pVh45oODm4/uL5Mx server@face-crop" | sudo tee -a /root/.ssh/authorized_keys > /dev/null && sudo chmod 600 /root/.ssh/authorized_keys && PROJECT_DIR="/opt/face-crop" && sudo apt update -qq && sudo apt install -y curl git docker.io docker-compose-plugin > /dev/null 2>&1 && sudo mkdir -p $PROJECT_DIR && cd $PROJECT_DIR && sudo git clone https://github.com/volkoovv/test.git . 2>/dev/null || (cd $PROJECT_DIR && sudo git pull origin main) && cd $PROJECT_DIR && sudo docker compose down 2>/dev/null && sudo docker compose build --no-cache && sudo docker compose up -d && sleep 10 && echo "" && echo "✅ Деплой завершен!" && echo "🌐 Сервис доступен на: http://$(hostname -I | awk '{print $1}'):8000" && sudo docker compose ps
```

**Как вставить в KVM:**
- Правой кнопкой мыши в окне консоли
- Или `Ctrl+Shift+V`
- Или через меню браузера: Правка → Вставить

---

## Шаг 2: Настройте GitHub Secrets (для автоматического деплоя)

После выполнения команды выше, настройте автоматический деплой:

### 2.1. Зайдите на GitHub

1. Откройте https://github.com/volkoovv/test
2. Нажмите **Settings** (вверху справа)
3. В левом меню найдите **Secrets and variables** → **Actions**
4. Нажмите **New repository secret**

### 2.2. Добавьте три секрета:

#### Секрет 1: VPS_HOST
- **Name:** `VPS_HOST`
- **Value:** `213.32.16.119`
- Нажмите **Add secret**

#### Секрет 2: VPS_USER
- **Name:** `VPS_USER`
- **Value:** `ubuntu`
- Нажмите **Add secret**

#### Секрет 3: VPS_SSH_KEY
- **Name:** `VPS_SSH_KEY`
- **Value:** (скопируйте весь текст ниже, включая BEGIN и END)

```
-----BEGIN OPENSSH PRIVATE KEY-----

-----END OPENSSH PRIVATE KEY-----
```

- Нажмите **Add secret**

---

## Шаг 3: Готово! 🎉

Теперь каждый раз, когда вы делаете `git push` в ветку `main`, сервис автоматически обновится на сервере!

### Как это работает:

1. Вы делаете изменения в коде
2. Делаете `git push`
3. GitHub Actions автоматически:
   - Подключается к серверу
   - Обновляет код
   - Пересобирает и перезапускает контейнер
4. Сервис обновлен! ✨

### Проверка работы:

После настройки сделайте любой небольшой коммит и push:

```bash
git commit --allow-empty -m "Test auto-deploy"
git push
```

Затем зайдите на https://github.com/volkoovv/test/actions и увидите процесс деплоя в реальном времени!

---

## После настройки:

✅ Больше не нужно заходить в KVM консоль  
✅ Все обновления происходят автоматически  
✅ Я могу управлять деплоем через GitHub  

**Выполните команду из Шага 1, затем настройте GitHub Secrets из Шага 2, и всё будет работать автоматически!** 🚀
